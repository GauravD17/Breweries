<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Brewery</title>
    <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>
<body>
   
  
    <div class="container">
      <h1>Random Breweries</h1>
        <div class="brewery-info">
          <h2> Breweries... </h2>
          <p><span class="field">ID:</span> <%= brewaryData.id %></p>
          <p><span class="field">Name:</span> <%= brewaryData.name %></p>
          <p><span class="field">City:</span> <%= brewaryData.city %></p>
          <p><span class="field">State:</span> <%= brewaryData.state %></p>
          <p><span class="field">Type:</span> <%=brewaryData.brewery_type%></p>
          <p><span class="field">Postal Code:</span> <%=brewaryData.postal_code %></p>
          <p><span class="field">state Province:</span> <%=brewaryData.state_province %></p>
          <p><span class="field">Country:</span> <%=brewaryData.country %></p>
          <p><span class="field">Phone:</span> <%= brewaryData.phone %></p>
        </div>
      </div>
    
</body>
</html>